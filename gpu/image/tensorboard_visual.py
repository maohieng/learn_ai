# tensorboard remove profiler, use Perfetto UI or chrome://tracing instead
# See https://pytorch.org/tutorials/intermediate/tensorboard_profiler_tutorial.html

# Install tensorboard via conda using the following command:
# conda install -c conda-forge tensorboard

# Install tensorboard-plugin-profile via conda using the following command:
# conda install -c conda-forge torch_tb_profiler

# Read the ./logs tensorboard logs using the following command:
# tensorboard --logdir ./logs

# Run the script and check the tensorboard logs
# python tensorboard_visual.py

# Read the ./logs tensorboard logs using p
